import 'package:flutter/material.dart';

class MatchPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Match')),
      body: Center(child: Text('Match Page')),
    );
  }
}
